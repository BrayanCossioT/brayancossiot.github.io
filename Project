<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Grid Layout con Editor Modal</title>
  <style id="dynamic-style">
    body {
      background-color: black;
      font-family: 'Times New Roman', Times, serif;
      margin: 0;
    }

    .container {
      display: grid;
      grid-template-rows: 80px auto 1fr auto minmax(50px, auto);
      /* ✅ Footer flexible */
      grid-template-columns: 1fr 4fr 1fr;
      min-height: 100vh;
      /* ✅ ocupa al menos la altura de pantalla */
      grid-template-areas:
        "header         header        header"
        "left-aside     banner        right-aside"
        "left-aside     main          right-aside"
        "left-aside     low-content   right-aside"
        "footer         footer        footer";
      grid-gap: 10px;
      padding: 10px;
      font-weight: 600;
      font-size: 20px;
      text-align: center;
      color: rgb(0, 0, 0);
    }

    header {
      grid-area: header;
      background: orange;
    }

    .banner {
      grid-area: banner;
      background: red;
    }

    .left-aside {
      grid-area: left-aside;
      background: green;
    }

    .right-aside {
      grid-area: right-aside;
      background: gold;
    }

    main {
      grid-area: main;
      background: dodgerblue;
    }

    .low-content {
      grid-area: low-content;
      background: teal;
    }

    footer {
      grid-area: footer;
      background: blue;
    }

    @media(max-width:678px) {
      .container {
        display: grid;
        grid-template-columns: 1fr;
        grid-template-rows: auto auto auto 1fr auto auto auto;
        /* main ocupa más espacio con 1fr */
        grid-template-areas:
          "header"
          "banner"
          "left-aside"
          "main"
          "right-aside"
          "low-content"
          "footer";
        grid-gap: 15px;
        padding: 15px;
      }

      main {
        min-height: 60vh;
        /* ✅ asegura que main se vea grande */
      }

      .container>* {
        border-radius: 3px;
        padding: 10px;
      }
    }



    /* 🔹 Botón flotante */
    #open-editor {
      position: fixed;
      bottom: 20px;
      right: 20px;
      padding: 12px 18px;
      background: #ff6600;
      border: none;
      border-radius: 3px;
      font-size: 20px;
      cursor: pointer;
      color: white;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
      z-index: 1000;
    }

    /* 🔹 Fondo difuminado */
    .overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.6);
      backdrop-filter: blur(5px);
      display: none;
      justify-content: center;
      align-items: center;
      z-index: 999;
    }

    /* 🔹 Ventana del editor */
    .editor-modal {
      background: #111;
      color: white;
      padding: 20px;
      border-radius: 3px;
      width: 80%;
      max-width: 800px;
      height: 60%;
      display: flex;
      flex-direction: column;
    }

    .editor-modal textarea {
      flex: 1;
      width: 100%;
      font-family: monospace;
      background: #222;
      color: #0f0;
      border: none;
      padding: 10px;
      resize: none;
      border-radius: 5px;
    }

    .editor-modal button {
      margin-top: 10px;
      padding: 8px 12px;
      border: none;
      background: crimson;
      color: white;
      cursor: pointer;
      border-radius: 5px;
      align-self: flex-end;
    }

    /* Ocultar scrollbars (opcional) */
    html::-webkit-scrollbar,
    body::-webkit-scrollbar {
      display: none;
    }
  </style>
</head>

<body>
  <div class="container">
    <header>
      Header
    </header>
    <div class="left-aside">
      Left Aside
    </div>
    <div class="banner">
      Banner
    </div>
    <div class="right-aside">
    Right Aside
    </div>
    <main>
      Main Content
    </main>
    <div class="low-content">
      Low Content
    </div>
    <footer>
      Footer
    </footer>
  </div>

  <button id="open-editor">🤡</button>

  <!-- Overlay + Editor -->
  <div class="overlay" id="overlay">
    <div class="editor-modal">
      <h3>Editor de estilos</h3>
      <textarea id="editor"></textarea>
      <button id="close-editor">Cerrar</button>
    </div>
  </div>

  <script>
    const styleTag = document.getElementById("dynamic-style");
    const editor = document.getElementById("editor");
    const overlay = document.getElementById("overlay");
    const openBtn = document.getElementById("open-editor");
    const closeBtn = document.getElementById("close-editor");

    // Cargar estilos actuales
    editor.value = styleTag.textContent;

    // Actualizar CSS en tiempo real
    editor.addEventListener("input", () => {
      styleTag.textContent = editor.value;
    });

    // Abrir modal
    openBtn.addEventListener("click", () => {
      overlay.style.display = "flex";
    });

    // Cerrar modal
    closeBtn.addEventListener("click", () => {
      overlay.style.display = "none";
    });
  </script>
</body>

</html>
